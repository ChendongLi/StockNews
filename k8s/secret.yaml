# Template only — do not commit real values.
# To apply with real secrets:
#   kubectl create secret generic stocknews-secrets -n stocknews \
#     --from-literal=RECIPIENTS=... \
#     --from-literal=AGENTMAIL_API_KEY=... \
#     --from-literal=ANTHROPIC_API_KEY=... \
#     --from-literal=BRAVE_API_KEY=...
apiVersion: v1
kind: Secret
metadata:
  name: stocknews-secrets
  namespace: stocknews
type: Opaque
stringData:
  RECIPIENTS: "REPLACE_ME"
  AGENTMAIL_API_KEY: "REPLACE_ME"
  ANTHROPIC_API_KEY: "REPLACE_ME"
  BRAVE_API_KEY: "REPLACE_ME"
